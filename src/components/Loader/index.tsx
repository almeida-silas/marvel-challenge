import React from 'react';

import Spinner from 'react-native-spinkit';

const Loader: React.FC = () => {
  return <Spinner type="WanderingCubes" color="#3949ab" size={100} />;
};

export default Loader;
